# Core Web Dependencies
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Financial Data
yfinance>=0.2.36

# Machine Learning (TimeMixer Engine)
neuralforecast>=1.7.0

# Critical Fix for Render: Force the ultra-lightweight CPU version of PyTorch!
# This prevents the Render server from crashing trying to download 3GB of GPU drivers.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
